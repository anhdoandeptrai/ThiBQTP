name: thucpham
description: "A new Flutter project."

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  image_picker: ^0.8.6
  image: ^3.0.1
  get: ^4.6.5
  cupertino_icons: ^1.0.6
  tflite: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/strawberry_classifier.tflite
    - assets/labels.txt
    - assets/images/strawberry_background.jpg
